from django.shortcuts import render
from .models import Submission

def home(request):
    submissions = Submission.objects.all().order_by('-uploaded_at')
    return render(request, 'base.html', {'submissions': submissions})
